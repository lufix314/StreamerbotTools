{
  "name": "streamerbot_tools",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js watch & python -m http.server 3000 -d build",
    "clean": "rm -rf build/*"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "esbuild-plugin-alias": "^0.2.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@streamerbot/client": "^1.12.2"
  }
}
